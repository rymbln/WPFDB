<UserControl x:Class="WPFDB.View.PersonWorkspaceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:v="clr-namespace:WPFDB.View"
      xmlns:vm="clr-namespace:WPFDB.ViewModel"
      xmlns:p="clr-namespace:WPFDB.Properties"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
>

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:PersonViewModel}">
            <v:PersonDetailView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <HeaderedContentControl Style="{StaticResource SubDisplayArea}" Grid.Column="0">
            <HeaderedContentControl.Header>
                Persons
            </HeaderedContentControl.Header>
            <ScrollViewer>
                <HeaderedContentControl>
                    <HeaderedContentControl.Header>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <ToolBar>
                                <Button Command="{Binding AddPersonCommand}">
                                    <AccessText Text="{x:Static p:Resources.AddMenu}"/>
                                </Button>
                                <Button Command="{Binding DeletePersonCommand}">
                                    <AccessText Text="{x:Static p:Resources.RemoveMenu}"/>
                                </Button>
                                <Button Command="{Binding RefreshCommand}">
                                    <AccessText Text="{x:Static p:Resources.RefreshMenu}"/>
                                </Button>
                                <Separator/>
                                <Label Content="{x:Static p:Resources.SearchMenu}" Target="{Binding ElementName=searchBox}" Padding="5,0,7,0" Margin="0" 
                       VerticalAlignment="Center"/>
                                <TextBox x:Name="searchBox" Width="150" BorderBrush="LightGray" BorderThickness="1" 
                         Text="{Binding Path=FilterText, UpdateSourceTrigger=PropertyChanged}" />
                            </ToolBar>
                        </StackPanel>
                    </HeaderedContentControl.Header>

                    <DataGrid SelectedItem="{Binding Path=CurrentPerson, Mode=TwoWay}" ItemsSource="{Binding Path=AllPersons}" AutoGenerateColumns="False" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="First Name" Binding="{Binding Path=FirstName}" />
                            <DataGridTextColumn Header="Second Name" Binding="{Binding Path=SecondName}" />
                            <DataGridTextColumn Header="Third Name" Binding="{Binding Path=ThirdName}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </HeaderedContentControl>
            </ScrollViewer>
        </HeaderedContentControl>

        <HeaderedContentControl Style="{StaticResource SubDisplayArea}" Margin="10,0,0,0" Grid.Column="1">
            <HeaderedContentControl.Header>
                Person Detail
            </HeaderedContentControl.Header>
            <ScrollViewer>
                <StackPanel>
                    <Border Style="{StaticResource DetailBorder}" Margin="5">
                        <ContentControl HorizontalAlignment="Right" Content="{Binding Path=CurrentPerson}" />
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </HeaderedContentControl>
    </Grid>
</UserControl>
